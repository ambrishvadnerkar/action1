name: My First Workflow

on: [push]

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Welcome message
        run: ¦
            echo "My First Github Action Job"
            ls -ltra
            cat README.md
      - name: Generate ASCII Artwork
        run: cowsat -f dragon "Run for cover, I am a DRAGON.....RAWR" >> dragon.txt
        
    #   - name: List files
    #     run: ls
    
    #   - name: Read file
    #     run: cat README.md
    
